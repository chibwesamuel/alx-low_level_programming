#include "main.h"
#include <stdio.h>

/**
 * _atoi - function that convert a string to an integer
 * @s: string
 * Return: integer
 */

int _atoi(char *s)
{
	char s[2] = {'4','10'};
	int i = _atoi(s);

	putchar(i);
	return (0);
}
