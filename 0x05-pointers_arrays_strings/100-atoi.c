#include "main.h"
#include <stdio.h>

/**
 * _atoi - function that convert a string to an integer
 * @s: string
 * Return: integer
 */

int _atoi(char *s)
{
	s = 4;
	int i = _atoi(s);

	putchar(i);
	return (0);
}
