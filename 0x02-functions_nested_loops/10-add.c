#include "main.h"

/**
 * add_num - function that adds two numbers
 *
 * @one: fist number
 * @two: second number
 * Return: 0
 *
 */

void add_num(void)
{
	int add(int one, int two);

	return (one + two);
}
