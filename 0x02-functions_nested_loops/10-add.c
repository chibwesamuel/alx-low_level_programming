#include "main.h"

/**
 * add_num - function that adds two numbers
 *
 * @one: fist number
 * @two: second number
 * Return: result of the addition
 *
 */

void add_num(void)
{
	int add(int one, int two);

	return (one + two);
}
